# --- CORE WEB ---
Flask==3.1.2
flask-cors==6.0.1
Flask-PyMongo==3.0.1
Flask-Login==0.6.3
Flask-WTF==1.2.2
gunicorn==23.0.0
python-dotenv==1.2.1
python-multipart==0.0.21
bcrypt==5.0.0

# --- DATABASE & STORAGE ---
pymongo==4.15.5
cloudinary==1.44.1

# --- GOOGLE AUTHENTICATION ---
# Leave these unpinned so they can find versions compatible with TF
google-auth
google-auth-oauthlib
requests-oauthlib
requests==2.32.5

# --- PEST DETECTION (Memory Optimized) ---
# Critical: Use -cpu version to stay under 512MB RAM
tensorflow-cpu==2.15.0
opencv-python-headless==4.8.1.78
pillow==12.0.0
# Keep numpy below 2.0 to avoid breaking TensorFlow 2.x
numpy>=1.23.5,<2.0.0
h5py==3.10.0

# --- AI APIs ---
google-generativeai==0.8.5